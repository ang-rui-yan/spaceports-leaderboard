listen_addresses = '*'              # listen on all interfaces
max_connections = 100               # increase if you expect more connections
shared_buffers = 256MB              # adjust based on your server's memory
work_mem = 4MB                      # increase for complex queries
maintenance_work_mem = 64MB         # increase for `VACUUM` operations
wal_level = replica                 # for replication setups
effective_cache_size = 2GB          # adjust based on available memory
